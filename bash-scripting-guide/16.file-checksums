File checksums:

- Short strings of data generated by hash functions to verify data integrity of a file.
- Any changes to the file will also change the checksum.
- Helps detect corruption, tampering or accidental changes.

Checksum commands:

md5sum "path/to/file"
or
sha256sum "path/to/file"

